@model ClinicaPro.Core.Features.RH.Queries.FuncionarioDto

@{
    ViewData["Title"] = "Excluir Funcionário";
}

<h1 class="mt-4 text-danger fw-bold">Excluir Funcionário</h1>

<h3 class="mt-3">Tem certeza que deseja desativar este funcionário?</h3>

<div class="alert alert-warning mt-3" role="alert">
  Atenção: A exclusão neste sistema é uma **desativação lógica** (o registro é mantido).
</div>

<div class="card shadow-sm border-0 mt-4">
    <div class="card-header bg-danger text-white">
        <h4 class="mb-0">Dados do Funcionário</h4>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-4 text-muted">@Html.DisplayNameFor(model => model.Nome)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.Nome)</dd>

            <dt class="col-sm-4 text-muted">@Html.DisplayNameFor(model => model.Sobrenome)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.Sobrenome)</dd>
            
            <dt class="col-sm-4 text-muted">@Html.DisplayNameFor(model => model.CPF)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.CPF)</dd>

            <dt class="col-sm-4 text-muted">@Html.DisplayNameFor(model => model.Cargo)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.Cargo)</dd>
            
            <dt class="col-sm-4 text-muted">@Html.DisplayNameFor(model => model.DataContratacao)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.DataContratacao)</dd>

            <dt class="col-sm-4 text-muted">Situação Atual</dt>
            <dd class="col-sm-8">
                 <span class="badge @(Model.Ativo ? "bg-success" : "bg-danger")">
                    @(Model.Ativo ? "Ativo" : "Inativo")
                </span>
            </dd>
        </dl>
    </div>
</div>

<form asp-action="Delete" method="post" class="mt-4">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger btn-lg me-2">
        <i class="bi bi-trash-fill"></i> Confirmar Desativação
    </button>
    <a asp-action="Index" class="btn btn-secondary btn-lg">
        <i class="bi bi-arrow-left"></i> Cancelar
    </a>
</form>