@model ClinicaPro.Core.Entities.Medico

@{
    ViewData["Title"] = "Detalhes do Médico";
}

<h1 class="mt-4">Detalhes do Médico</h1>

<div class="card shadow-sm mt-3">
    <div class="card-body">
        <dl class="row mb-0">
            <dt class="col-sm-3">Nome</dt>
            <dd class="col-sm-9 mb-3">@Model.Nome</dd>

            <dt class="col-sm-3">CRM</dt>
            <dd class="col-sm-9 mb-3">@Model.CRM</dd>

            <dt class="col-sm-3">Especialidade</dt>
            <dd class="col-sm-9 mb-3">@(Model.Especialidade?.Nome ?? "Não cadastrada")</dd>

            <dt class="col-sm-3">Email</dt>
            <dd class="col-sm-9 mb-3">@Model.Email</dd>

            <dt class="col-sm-3">Telefone</dt>
            <dd class="col-sm-9 mb-3">@Model.Telefone</dd>
        </dl>

        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning me-2">Editar</a>
        <a asp-action="Index" class="btn btn-secondary">Voltar</a>
    </div>
</div>
